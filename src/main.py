from wikiPT import call_wikiPT_api
from eventPT import call_eventPT_api
from utils.create_event import create_event
from datetime import datetime

# term, url = call_wikiPT_api("Die Integration von Pythoncode in C++ Programm führte zu konflikten deren Bewältigung länger dauerte als die eigentliche Integration.")

# print(term)
# print(url)

call_eventPT_api("Im nervous because next week im in Paris.")
